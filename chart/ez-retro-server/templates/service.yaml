apiVersion: v1
kind: Service
metadata:
  name: {{ include "ez-retro-server.fullname" . }}
  labels:
    {{- include "ez-retro-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ez-retro-server.selectorLabels" . | nindent 4 }}
